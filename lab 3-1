/* Zadanie 1:* przeanalizuj kod, wykryj błędy i wyjaśnij ich przyczynę wraz z odniesieniem się do komunikatów od kompilatora: */

void printtab(int n){
  static int tab[100*n];
}

int main(){
   int a[5] = {1,2,3,4,5};
   int b[] = {6,7,8,9,0};
   int c[5] = {0,2,3,4,5,6}
   int *ptr = a;
   
   a = b;
   b = ptr;

   return 0;
}
/*nie ma w mainie wartosci n, 
za dużo wartosci w tablicy c (jest ich 6, nie 5) + brak ; na końcu
nie ma zdefiniowanego ptr (jest *ptr, ale nie normalne ptr)
a = b - a i b są tablicami, prawidłowe wpisane a [jedna liczba od 1 do 5] = b [jedna liczba od 1 do 5]
błąd przy tab[100*n] - nieznana jest wartość n
